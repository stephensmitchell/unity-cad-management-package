// Place in Assets/ExtrudableObject.cs

using UnityEngine;

// This component marks an object as being editable by our custom CAD handle.
[RequireComponent(typeof(MeshFilter))]
public class ExtrudableObject : MonoBehaviour
{
}